{
  "version": 2,
  "buildCommand": "npm install --prefix academic-integrity-frontend && npm run build --prefix academic-integrity-frontend",
  "outputDirectory": "academic-integrity-frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
